#include <stdio.h>

/**
 * main - contains the entire function
 *
 * Return: returns 0 when the program has run
 */

int main(void)
{
	int j;

	for (j = 0 ; j <= 9 ; j++)
		printf("%d", j);
	printf("\n");
	return (0);
}
