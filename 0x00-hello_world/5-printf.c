#include <stdio.h>

/**
 * main - contains the entire function
 * Return: returns 0 when the function has run
 */

int main(void)
{
	printf("with propar grammar, but the outcome is a piece of art,\n");
	return (0);
}
